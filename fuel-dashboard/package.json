{
    "name": "fuel-dashboard-ui-tests",
    "private": true,
    "scripts": {
        "ui:test": "playwright test",
        "ui:test:headed": "playwright test --headed",
        "ui:test:debug": "playwright test --debug"
    },
    "devDependencies": {
        "@playwright/test": "^1.55.0",
        "@types/node": "^24.0.1",
        "typescript": "^5.8.3"
    }
}
